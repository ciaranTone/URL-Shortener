apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.35.0 (9532ceef3)
  labels:
    io.kompose.service: app
  name: app
spec:
  type: NodePort
  ports:
    - name: "backend-port"
      port: 8080
      targetPort: 8080
      nodePort: 30002
  selector:
    io.kompose.service: app
